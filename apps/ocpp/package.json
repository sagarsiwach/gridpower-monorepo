{
  "name": "@grid-platform/ocpp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "PORT=14781 bun --watch src/server.ts",
    "start": "PORT=14781 bun src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@grid-platform/env": "workspace:*",
    "@grid-platform/observability": "workspace:*",
    "hono": "4.12.27",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
